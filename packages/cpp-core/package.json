{
  "name": "@napoleon/cpp-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cmake -S native -B build && cmake --build build",
    "test": "pnpm run build && ctest --test-dir build --output-on-failure && pnpm --filter @napoleon/game-core build && tsc -p tsconfig.json && bash test/run_differential.sh",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@napoleon/game-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
