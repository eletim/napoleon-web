# Comma-separated host names allowed to access the Vite development server.
# Keep machine-specific values in .env.local.
# Example: VITE_ALLOWED_HOSTS=my-machine.example.ts.net,100.64.0.1
VITE_ALLOWED_HOSTS=
